package elevate.autotuning_search

class AutotuningSearch {

  // tune vector
  // first rewrite
  // second rewrite

  // a = [0,10] -> first rewrite, which rule as first?
  // b = [0,10] | b < a
  // c = [0,10] |

  // 0 -> split join
  // 1 -> lowerGS
  // 2 -> lowerGSGS

  // a = [0, 1, 2] | a < 2
  // b = [0, 1, 2] | ?

  // create search space breadth first until certain size using elevate strategies
  // store search space in data structure

  // map points of search space created by rewrite rules to numerical tuning parameters -> or ordinal, look for HM, cot?

  // tune parameter space

  // client server hm
  // pick program

  // tuning:
  // get parameter configuration
  // pick expression based on parameter configs

  // execute expression -> auto tune expression
  // report runtime


}
